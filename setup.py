# setup.py
from setuptools import setup

setup(
    name='pydom',
    version='0.1.0',
    packages=['pydom'],
    install_requires=[
        "scipy", 
        "matplotlib",
        "numpy",
    ],
)
