# setup.py
from setuptools import setup

setup(
    name='pydyn',
    version='0.1.0',
    packages=['pydyn'],
    install_requires=[
        "scipy", 
        "matplotlib",
        "numpy",
    ],
)
